# Employee-Database-MYSQL-Module-2
Extends the Employee Database from Assignment 1 to demonstrate SQL querying: filtering, sorting, aggregation with GROUP BY/HAVING, all join types (INNER, LEFT, RIGHT, CROSS, SELF), and window functions (RANK, DENSE_RANK, running totals) — using 30 employee records across 13 departments to answer real-world business questions.
